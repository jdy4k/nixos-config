{ pkgs, input, myconfig, ... }:
{
  programs.hyprlock = {
    settings = {
      general = {
        no_fade_in = true;
        no_fade_out = true;
        hide_cursor = false;
        grace = 0;
        disable_loading_bar = true;
      };

      background = [
        {
          path = "${./../../../../../../assets/hyprlock-bg.jpg}";
          blur_passes = 2;
          contrast = 1;
          brightness = 0.5;
          vibracy = 0.2;          
          vibracy_darkness = 0.2;
        }
      ];

      input-field = [
        {
          monitor = "${myconfig.monitors.primary.name}";
          placeholder_text = '''';
          size = "400, 60";
          outline_thickness = 2;
          dots_size = 0.2; # Scale of input-field height, 0.2 - 0.8
          dots_spacing = 0.35; # Scale of dots' absolute size, 0.0 - 1.0
          dots_center = true;
          outer_color = "rgba(50, 50, 80, 0)";
          inner_color = "rgba(50, 50, 80, 0.5)";
          font_color = "rgba(255, 255, 255, 1)";
          fade_on_empty = true;
          rounding = -1;
          check_color = "rgb(204, 136, 34)";
          hide_input = false;
          position = "0, -500";
          halign = "center";
          valign = "center";
        }
      ];

      label = [
        {
          monitor = "${myconfig.monitors.primary.name}";
          text = ''cmd[update:1000] echo "$(date +"%A, %B %d")"'';
          color = "rgba(242, 243, 244, 0.75)";
          font_size = 40;
          font_weight = 0;
          font_family = "SFProDisplay Nerd Font Medium";
          position = "0, -600";
          halign = "center";
          valign = "top";
        }
        {
          monitor = "${myconfig.monitors.primary.name}";
          text = ''cmd[update:1000] echo "$(date +"%-I:%M")"'';
          color = "rgba(242, 243, 244, 0.75)";
          font_size = 120;
          font_family = "SFProDisplay Nerd Font SemiBold";
          font_weight = 0;
          position = "0, -400";
          halign = "center";
          valign = "top";
        }
      ];
    };
  };
}
